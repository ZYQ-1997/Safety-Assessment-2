# 修复公网访问"Failed to fetch"问题

## 问题原因

部署到公网后，前端代码中的API地址仍然使用 `localhost`，导致无法连接到后端。

## 已修复的问题

1. ✅ **前端API地址配置**：已改为使用相对路径 `/api`，自动适配当前域名
2. ✅ **CORS配置**：已优化后端CORS设置，允许跨域请求

## 需要重新部署

修复后需要重新部署代码：

### 如果使用Railway：

1. **提交代码到GitHub**：
   ```bash
   git add .
   git commit -m "Fix API URL for production"
   git push origin main
   ```

2. **Railway会自动重新部署**（等待2-5分钟）

3. **测试访问**：访问您的Railway域名，测试上传功能

### 如果使用其他平台：

1. 提交代码到GitHub
2. 平台会自动重新部署
3. 或者手动触发重新部署

## 验证修复

部署完成后：

1. 打开浏览器开发者工具（F12）
2. 查看Console标签，应该看到：
   ```
   当前访问方式: https://your-app.railway.app/
   API地址: /api
   ```

3. 尝试上传文件，查看Network标签：
   - 请求URL应该是：`https://your-app.railway.app/api/upload`
   - 不应该包含 `localhost`

## 如果问题仍然存在

### 检查1: 后端服务是否正常运行

访问：`https://your-app.railway.app/api/health`

应该返回：
```json
{"status": "ok"}
```

### 检查2: 查看浏览器控制台错误

1. 按F12打开开发者工具
2. 查看Console标签的错误信息
3. 查看Network标签，检查请求是否发送成功

### 检查3: 检查Railway部署日志

1. 在Railway项目页面
2. 点击 "Deployments"
3. 查看最新的部署日志
4. 确认服务已成功启动

### 检查4: 文件大小限制

如果上传大文件失败，可能是：
- Railway有文件大小限制
- 后端配置的文件大小限制

检查 `backend/app.py` 中的 `MAX_CONTENT_LENGTH` 设置。

## 常见错误及解决方案

### 错误1: CORS错误

**错误信息**：`Access to fetch at '...' from origin '...' has been blocked by CORS policy`

**解决方案**：已修复CORS配置，重新部署即可

### 错误2: 404 Not Found

**错误信息**：`Failed to fetch` 或 `404`

**解决方案**：
- 检查API路径是否正确
- 确认后端服务已启动
- 检查Railway的部署状态

### 错误3: 超时错误

**错误信息**：`Request timeout`

**解决方案**：
- Railway的免费计划可能有超时限制
- 尝试上传较小的文件测试
- 考虑升级到付费计划

## 测试步骤

1. ✅ 访问您的应用URL
2. ✅ 打开浏览器开发者工具（F12）
3. ✅ 查看Console，确认API地址正确
4. ✅ 尝试上传一个小文件（< 10MB）
5. ✅ 查看Network标签，确认请求成功
6. ✅ 如果成功，尝试上传更大的文件

## 需要帮助？

如果问题仍然存在，请提供：
1. 浏览器控制台的错误信息
2. Network标签中的请求详情
3. Railway部署日志

我可以进一步帮您排查问题。

