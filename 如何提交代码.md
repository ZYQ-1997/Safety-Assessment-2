# 如何提交代码到GitHub

## 方法1: 使用GitHub Desktop（最简单，推荐）

### 步骤：

1. **下载GitHub Desktop**（如果还没有）：
   - 访问：https://desktop.github.com
   - 下载并安装

2. **打开GitHub Desktop**：
   - 登录您的GitHub账号

3. **添加仓库**：
   - 点击 "File" → "Add Local Repository"
   - 选择项目文件夹：`C:\Users\Z2200\Desktop\Safety Assessment`
   - 点击 "Add"

4. **提交更改**：
   - 在左侧会显示所有更改的文件
   - 在底部输入提交信息：`Fix API URL for production deployment`
   - 点击 "Commit to main"

5. **推送到GitHub**：
   - 点击 "Push origin" 按钮
   - 如果提示登录，使用GitHub账号登录

完成！Railway会自动检测并重新部署。

## 方法2: 使用命令行（如果已安装Git）

### 步骤：

1. **打开Git Bash**（或命令提示符）

2. **进入项目目录**：
   ```bash
   cd "C:\Users\Z2200\Desktop\Safety Assessment"
   ```

3. **检查状态**：
   ```bash
   git status
   ```

4. **添加所有文件**：
   ```bash
   git add .
   ```

5. **提交更改**：
   ```bash
   git commit -m "Fix API URL for production deployment"
   ```

6. **推送到GitHub**：
   ```bash
   git push origin main
   ```

如果提示输入密码，请使用GitHub的Personal Access Token。

## 方法3: 使用批处理脚本

运行 `提交代码.bat` 文件，按提示操作即可。

## 如果遇到问题

### 问题1: 未安装Git

**解决方案**：
- 下载安装Git：https://git-scm.com/download/win
- 或使用GitHub Desktop（推荐，更简单）

### 问题2: 推送权限错误

**解决方案**：
- 运行 `快速修复推送.bat`
- 或使用GitHub Desktop推送

### 问题3: 未配置远程仓库

**解决方案**：
```bash
git remote add origin https://github.com/您的用户名/您的仓库名.git
```

## 验证提交

提交成功后：

1. 访问您的GitHub仓库页面
2. 应该能看到最新的提交
3. 如果使用Railway，等待2-5分钟，Railway会自动重新部署

## 需要帮助？

如果遇到问题，请告诉我具体的错误信息，我可以帮您解决。

