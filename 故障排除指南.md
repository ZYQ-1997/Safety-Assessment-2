# 前端页面无法访问 - 故障排除指南

## ✅ 服务器状态检查

服务器已确认正在运行：
- ✓ 端口5000已占用
- ✓ API健康检查正常（状态码200）
- ✓ 前端页面可以访问（状态码200）

## 🔍 如果仍然无法访问，请按以下步骤排查：

### 1. 确认访问地址
**正确地址**：http://localhost:5000

**错误地址示例**：
- ❌ http://127.0.0.1:5000 （虽然应该可以，但建议用localhost）
- ❌ file:///C:/Users/.../frontend/index.html （直接打开HTML文件）
- ❌ http://localhost:5000/frontend/index.html

### 2. 清除浏览器缓存
1. 按 `Ctrl + Shift + Delete` 打开清除数据对话框
2. 选择"缓存的图片和文件"
3. 点击"清除数据"
4. 刷新页面（按 `Ctrl + F5` 强制刷新）

### 3. 检查浏览器控制台
1. 按 `F12` 打开开发者工具
2. 查看"控制台"标签页是否有错误
3. 查看"网络"标签页，检查请求是否成功

### 4. 尝试不同的浏览器
- Chrome
- Edge
- Firefox

### 5. 检查防火墙设置
Windows防火墙可能阻止了连接：
1. 打开"Windows安全中心"
2. 点击"防火墙和网络保护"
3. 检查是否阻止了Python或端口5000

### 6. 检查是否有其他程序占用5000端口
运行 `检查服务器状态.bat` 查看端口状态

### 7. 重启服务器
1. 关闭当前运行的服务器（在命令行窗口按 `Ctrl + C`）
2. 重新运行 `启动服务器.bat`
3. 等待看到 "Running on http://0.0.0.0:5000" 消息

### 8. 查看服务器日志
查看运行服务器的命令行窗口，检查是否有错误信息

## 🛠️ 快速诊断命令

在PowerShell中运行以下命令：

```powershell
# 检查服务器是否响应
Invoke-WebRequest -Uri "http://localhost:5000" -UseBasicParsing

# 检查API是否正常
Invoke-WebRequest -Uri "http://localhost:5000/api/health" -UseBasicParsing

# 检查端口占用
netstat -ano | findstr :5000
```

## 📞 如果问题仍然存在

请提供以下信息：
1. 浏览器控制台的错误信息（F12 -> 控制台）
2. 网络标签页的请求状态（F12 -> 网络）
3. 服务器命令行窗口的输出
4. 使用的浏览器和版本

