# 🚀 最简单部署指南（无需Git，5分钟完成）

## 方案1: 使用Railway（推荐，最简单）

### 方式A: 通过网页直接部署（无需Git）

1. **访问 Railway**: https://railway.app
2. **使用GitHub账号登录**（如果没有GitHub账号，先注册一个：https://github.com）
3. **点击 "New Project"**
4. **选择 "Empty Project"**
5. **点击项目设置，选择 "Deploy from GitHub repo"**
   - 如果没有GitHub仓库，先创建一个：
     - 访问 https://github.com/new
     - 创建新仓库（可以是私有的）
     - 上传项目文件（可以通过网页上传zip文件）

### 方式B: 使用Railway CLI（更简单）

1. **安装Railway CLI**:
   ```bash
   # Windows (使用PowerShell)
   iwr https://railway.app/install.sh | iex
   
   # 或者使用npm
   npm install -g @railway/cli
   ```

2. **登录Railway**:
   ```bash
   railway login
   ```

3. **初始化并部署**:
   ```bash
   railway init
   railway up
   ```

完成！Railway会自动部署并提供一个URL。

## 方案2: 使用Render（同样简单）

1. **访问 Render**: https://render.com
2. **使用GitHub账号登录**
3. **点击 "New" → "Web Service"**
4. **连接GitHub仓库**（如果没有，先创建并上传代码）
5. **配置**:
   - Environment: Docker
   - Build Command: (留空，Render会自动检测)
   - Start Command: (留空)
6. **点击 "Create Web Service"**

完成！Render会自动部署。

## 方案3: 使用Vercel（适合前端，但需要调整）

Vercel主要支持前端，需要将Flask后端分离。

## 📝 详细步骤（Railway网页部署）

### 步骤1: 准备GitHub仓库

1. 访问 https://github.com/new
2. 创建新仓库（名称随意，如：pdf-table-extractor）
3. 选择 "Private"（私有仓库）
4. 点击 "Create repository"

### 步骤2: 上传代码到GitHub

**方法1: 使用GitHub Desktop（推荐，最简单）**

1. 下载 GitHub Desktop: https://desktop.github.com
2. 安装并登录
3. 点击 "File" → "Add Local Repository"
4. 选择项目文件夹
5. 点击 "Publish repository"
6. 完成！

**方法2: 使用网页上传**

1. 在GitHub仓库页面，点击 "uploading an existing file"
2. 拖拽项目文件夹中的所有文件（除了uploads和outputs）
3. 点击 "Commit changes"

### 步骤3: 部署到Railway

1. 访问 https://railway.app
2. 点击 "Login" → 选择 "Login with GitHub"
3. 授权Railway访问GitHub
4. 点击 "New Project"
5. 选择 "Deploy from GitHub repo"
6. 选择您的仓库
7. Railway会自动检测Dockerfile并开始部署
8. 等待部署完成（通常2-5分钟）

### 步骤4: 获取访问地址

部署完成后：
1. 在Railway项目页面，点击 "Settings"
2. 找到 "Domains" 部分
3. 点击 "Generate Domain" 获取免费域名
4. 或者添加自定义域名

完成！现在可以通过提供的URL访问您的应用了！

## 🎯 推荐流程

**最简单的方式**：
1. 安装GitHub Desktop
2. 上传代码到GitHub
3. 在Railway部署（5分钟完成）

## 💡 提示

- Railway提供 **$5/月免费额度**（足够使用）
- 每次推送代码到GitHub，Railway会自动重新部署
- Railway自动配置HTTPS，无需额外配置
- 支持自定义域名

## ❓ 需要帮助？

如果遇到问题：
1. 查看Railway的部署日志
2. 确保Dockerfile正确
3. 检查所有必要的文件都已上传到GitHub

