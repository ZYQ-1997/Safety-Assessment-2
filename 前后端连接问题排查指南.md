# 前后端连接问题排查指南

## 问题现象
前端界面的操作在后端无响应，无法上传文件、提取表格等功能无法使用。

## 快速检查清单

### 1. 检查后端服务器是否运行
- ✅ 打开命令行，进入项目目录
- ✅ 运行 `cd backend && python app.py`
- ✅ 应该看到类似以下输出：
  ```
  ========================================
  PDF表格提取工具 - 后端服务器
  ========================================
  前端界面: http://localhost:5000
  API接口: http://localhost:5000/api
  ========================================
  ```
- ✅ 如果看到错误，请检查：
  - Python是否正确安装
  - 依赖包是否已安装（运行 `pip install -r requirements.txt`）
  - 5000端口是否被占用

### 2. 检查前端访问方式
**推荐方式**：通过后端服务器访问前端
- 在浏览器中访问：`http://localhost:5000`
- 这样前端会自动使用相对路径 `/api`，避免跨域问题

**备选方式**：直接打开HTML文件
- 如果直接打开 `frontend/index.html` 文件（file://协议）
- 前端会自动使用 `http://localhost:5000/api` 作为API地址
- **必须确保后端服务器正在运行**

### 3. 使用浏览器诊断工具

#### 方法1：查看浏览器控制台
1. 按 `F12` 打开开发者工具
2. 切换到 "Console"（控制台）标签
3. 查看是否有错误信息
4. 应该看到类似以下信息：
   ```
   当前访问方式: http://localhost:5000
   API地址: /api
   ✅ 后端连接测试成功: 200
   ```

#### 方法2：运行诊断函数
在浏览器控制台中输入：
```javascript
diagnoseConnection()
```
这会运行详细的连接测试，并显示结果表格。

#### 方法3：检查网络请求
1. 在开发者工具中切换到 "Network"（网络）标签
2. 尝试上传文件或执行操作
3. 查看是否有失败的请求（红色）
4. 点击失败的请求查看详细信息：
   - 状态码（Status Code）
   - 错误信息（Error Message）
   - 请求URL是否正确

### 4. 常见错误及解决方法

#### 错误1：`Failed to fetch` 或 `无法连接到后端服务器`
**原因**：后端服务器未运行或无法访问

**解决方法**：
1. 检查后端服务器是否正在运行
2. 确认后端运行在 `http://localhost:5000`
3. 检查防火墙是否阻止了连接
4. 尝试在浏览器中直接访问 `http://localhost:5000/api/health`

#### 错误2：`CORS policy` 相关错误
**原因**：跨域资源共享（CORS）配置问题

**解决方法**：
- 已修复：后端已配置允许所有来源的CORS
- 如果仍有问题，确保使用最新版本的后端代码

#### 错误3：`404 Not Found` 或 `资源未找到`
**原因**：API路径不正确

**解决方法**：
1. 检查前端是否正确检测到API地址
2. 查看浏览器控制台中的 `API地址` 日志
3. 确保后端路由正确注册（查看后端启动时的路由列表）

#### 错误4：`请求超时` 或 `AbortError`
**原因**：请求处理时间过长或网络问题

**解决方法**：
1. 检查文件大小是否过大（最大500MB）
2. 检查网络连接
3. 对于大文件，处理可能需要较长时间，请耐心等待

### 5. 手动测试后端API

#### 测试健康检查端点
在浏览器中访问：
```
http://localhost:5000/api/health
```
应该返回：
```json
{
  "status": "ok",
  "message": "服务运行正常"
}
```

#### 测试文件上传（使用curl）
```bash
curl -X POST http://localhost:5000/api/upload -F "file=@test.pdf"
```

### 6. 检查端口占用

#### Windows
```powershell
netstat -ano | findstr :5000
```

#### Linux/Mac
```bash
lsof -i :5000
```

如果端口被占用：
1. 找到占用端口的进程ID（PID）
2. 结束该进程，或
3. 修改后端代码使用其他端口（需要同时修改前端API地址）

### 7. 检查依赖包

确保所有依赖包已安装：
```bash
pip install -r requirements.txt
```

关键依赖包：
- flask
- flask-cors
- pdfplumber
- pandas
- openpyxl
- pypdf

### 8. 查看后端日志

后端服务器会在控制台输出详细的日志信息：
- 请求路径
- 错误信息
- 处理进度

如果遇到错误，查看后端控制台的错误信息，通常包含详细的堆栈跟踪。

## 修复后的改进

### 前端改进
1. ✅ 改进了API地址自动检测逻辑
2. ✅ 添加了详细的错误处理和用户提示
3. ✅ 添加了连接状态检查和诊断工具
4. ✅ 改进了网络请求的超时处理
5. ✅ 添加了更详细的调试日志

### 后端改进
1. ✅ 改进了CORS配置，支持所有来源
2. ✅ 添加了详细的错误处理
3. ✅ 改进了错误响应格式

## 如果问题仍然存在

1. **清除浏览器缓存**：按 `Ctrl+Shift+Delete` 清除缓存
2. **尝试不同浏览器**：Chrome、Firefox、Edge等
3. **检查浏览器控制台**：查看是否有JavaScript错误
4. **查看后端日志**：后端控制台会显示详细的错误信息
5. **重启后端服务器**：停止并重新启动后端服务

## 联系支持

如果以上方法都无法解决问题，请提供以下信息：
1. 浏览器控制台的完整错误信息
2. 后端服务器的完整日志
3. 操作系统和Python版本
4. 使用的访问方式（http://localhost:5000 或 file://）

